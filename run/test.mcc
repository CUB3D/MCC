; Bootstrap instruction
mov PC, ML
mov RAM, IR

; TODO support mov [PC], IR
; TODO support padding to position based on current loc
.fill 0xF
; Read RAM to A
mov IR, ML
mov RAM, A
inc PC
; TODO support bracket syntax
mov PC, ML
mov RAM, IR

.fill 0xB
; Read RAM to B
mov IR, ML
mov RAM, B
inc PC
; TODO support bracket syntax
mov PC, ML
mov RAM, IR

.fill 0xC
; Read ALU to A
mov ALU, A
inc PC
; TODO support bracket syntax
mov PC, ML
mov RAM, IR